# 0429 TODO List

> 日期：2026-04-29 | 优先级从高到低排列

---

## 一、BUG 修复

### 1.1 第二章大殿文字消失 🔴 高优

**问题**：进入第二章到了大殿之上的文字消失了，无法看到具体的对话。

**可能原因**：
- `StoryScreen.ts` 中 `_showDialogue()` 或 `_showNarration()` 的文字渲染逻辑问题
- `style.css` 中 `story-dialogue-text` 或 `story-narration-text` 的 `color` / `visibility` / `z-index` 被覆盖
- `story-bg` 的 `dialogue-bg` class 覆盖了文字区域
- 检查 `story-overlay` 的 `z-index` 是否遮挡了文字层

**任务**：
- 检查 `src/screens/StoryScreen.ts` 中 `_showDialogue()` 和 `_showNarration()` 函数
- 检查 `src/style.css` 中 `.story-dialogue-mode`、`.story-dialogue-text`、`.story-narration-text` 的样式
- 确认文字颜色（`var(--text-light)`）在暗色背景下是否可见
- 确认 `story-bg.dialogue-bg` 的 `height: 100%` 是否覆盖了文字元素

---

### 1.2 经验曲线优化——控制升级最多20次点击 🔴 高优

**问题**：目前的修为等级提升太慢。切磋一下只 +35 经验，但八层到九层的经验需要 1131，这需要大约 35 次点击，过于枯燥。

**目标**：优化到最多的升级也控制在 20 次以内。

**当前数据**（供参考）：
- 当前公式：`getExpForLevel(lv)` 分段：lv≤10 用 `50×lv^1.5`，lv 11~30 用 `50×lv^1.3`，lv31+ 用 `50×lv^1.15`
- 演武切磋：+35 经验（最高日常任务）
- 炼气八层→九层：需要 1131 经验，约 35 次切磋

**任务**：
- 修改 `src/state/LevelSystem.ts` 中的 `getExpForLevel()` 函数
- 建议方案（二选一或组合）：
  - **方案A**：进一步降低经验公式指数（如 `lv^1.1` 或更低），使最高等级经验需求 ≤ 700
  - **方案B**：大幅提高日常任务经验奖励（如演武切磋从 +35 提升至 +60~80），使最多 20 次点击即可升级
  - **方案C**：两者结合——降低经验曲线 + 提高任务奖励
- 验证：计算每个等级从当前经验到下一级所需切磋次数，确保 ≤ 20 次
- 同时考虑战斗经验获取速率，确保整体节奏合理

---

### 1.3 多人战斗 NPC 队友 AI 卡住 🔴 高优

**问题**：第二章下山剿匪，主角和顾小桑对战四个土匪时，顾小桑行动中无法继续下去（战斗卡住）。

**根因分析**：
- 当前 `BattleEngine.ts` 中 `_advanceTurn()` 对非玩家友方单位（`ally` side 但 `isPlayer: false`）的处理逻辑缺失
- 在 `_advanceTurn()` 第 255 行：`_ctx.state = nextUnit.side === 'ally' ? 'player_turn' : 'enemy_turn';`
- 所有 ally 单位都被设为 `player_turn`，但非玩家控制的队友没有 AI 逻辑驱动
- 结果：轮到顾小桑时进入 `player_turn` 等待玩家输入，但玩家无法控制她 → 卡死

**任务**：
- 修改 `src/systems/BattleEngine.ts` 中的 `_advanceTurn()` 函数
- 当轮到非玩家控制的友方单位（`side === 'ally' && !isPlayer`）时，应执行队友 AI 逻辑
- 队友 AI 行为建议：
  - 匹配 NPC 设定，使用其 `skills` 数组中的技能随机释放
  - 优先攻击血量最低的敌人
  - MP 不足时使用普通攻击
  - 可参考 `EnemyAI.ts` 的加权随机模式，为队友技能设置权重
- 修改 `src/systems/EnemyAI.ts`（或新建 `AllyAI.ts`）：
  - 新增 `selectAllyAction(ally: BattleUnit): { skillId: SkillId | null; targetId: string }` 函数
  - 逻辑：优先使用技能（随机选一个 MP 足够的），否则普通攻击；目标选 HP 最低的敌人
- 确保修改后不影响 1v1 战斗和敌方 AI 行为

---

## 二、机制开发

### 2.1 NPC 动态系统（大工程）🟢 新功能

#### 2.1.1 NPC 数值卡系统

**目标**：每个存档建立各人物的"数值卡"，与主角 `PlayerState` 结构一致。

**任务**：
- 新建 `src/data/npcStats.ts`（或类似文件），定义 NPC 数据库结构
- 数据结构参考：
  ```typescript
  interface NpcStats {
    id: string;
    name: string;
    level: number;        // 当前修为等级
    exp: number;
    hp/maxHp/mp/maxMp/atk/def/agi/crit: number;
    skills: SkillId[];    // 已学会的技能
    equippedFabao: { weapon, armor, accessory };  // 装备的法宝
    ownedFabao: FabaoId[];
    talents: TalentId[];  // 天赋列表
    sect: SectId;
  }
  ```
- 定义各阶段"标准数值等级"（每个境界的基准属性）
- 定义天赋系统：
  - `sword_heart`（剑心）：修行速度 ×1.5（柳清寒）
  - `lazy`（贪玩）：修行速度 ×0.7（宋知远）
  - `diligent`（勤勉）：修行速度 ×1.2
  - `genius`（天才）：技能学习成功率 +20%
  - 等更多天赋待扩展

#### 2.1.2 NPC 每回合行为逻辑

**概述**：当前游戏暂未引入时间概念，暂且将"一回合"理解为一次存档/休息/关键事件触发。NPC 每回合执行以下判断：

##### 行为 1：购买法器（40% 概率）
- 触发条件：NPC 三件法器未全部装备
- 结果分布：
  - 20% — "淘到宝了"：购买到比自己高一阶的缺失法器
  - 30% — "一分钱一分货"：购买到与自己同阶的缺失法器
  - 50% — "来晚了"：未购买到法器

##### 行为 2：学习技能（30% 概率）
- 触发条件：NPC 未学满所在宗门同等级的全部技能
- 结果分布：
  - 50% — "成功领悟"：学会一门自己不会的同级技能
  - 50% — "领悟失败"：未能学会

##### 行为 3：修炼（剩余概率，补齐至 100%）
- 如果 NPC 不缺法宝也不缺技能，则修炼概率 = 100%
- 结果分布：
  - 10% — "天人合一"：获得 30 点修为
  - 80% — "修行"：获得 15 点修为
  - 10% — "走火入魔"：获得 5 点修为

##### 概率补齐逻辑
```
修炼概率 = 100% - (缺法宝 ? 40% : 0%) - (缺技能 ? 30% : 0%)
```

#### 2.1.3 实现要求

**任务**：
- 新建 `src/systems/NpcBehavior.ts` — NPC 行为引擎
- 新建 `src/data/npcStats.ts` — NPC 初始数值卡数据库
- 在 `src/state/schemas.ts` 中新增 NPC 数据库的 Zod Schema
- 在 `PlayerState` 或独立存储中持久化 NPC 状态
- 提供 API：`initNpcDatabase()` / `tickNpcBehaviors()` / `getNpcStats(npcId)`
- 存档加载时初始化或恢复 NPC 数据库

---

## 三、第三章后续工作

### 3.1 CG 图片需求 🟡 中优

**任务**：更新 `docs/CG_GENERATION_PLAN.md`，为第三章生成图片需求清单。

**当前第三章引用的 CG**（均待生成）：
| 文件名 | 用途 | 节点 |
|--------|------|------|
| `C3-neimen_xiaowu.png` | 内门小屋（月光透窗，书案剑谱） | 突破、婚约 |
| `C3-qingshicun.png` | 青石村密林（古木参天，妖气弥漫） | 下山行侠 |

**要求格式**（请手动添加图片）：
```
文件名：C3-xxx.png
文件夹：public/picture/scene/
提示词：[英文提示词，用于 AI 图片生成]
```

> 截至第二章，所有 CG 图片已生成完毕。第三章的图片需求请按上述格式列出，我会手动添加。

### 3.2 NPC 立绘需求 🟡 中优

第三章新增 NPC 目前使用占位图片（`picture/NPC/武当派-张三丰.png`），需要生成专属立绘：
- 纪无双、苏云绣、方仲和、孟文渊、叶紫衣

---

## 优先级总结

| 优先级 | 编号 | 内容 |
|--------|------|------|
| 🔴 高 | 1.1 | 第二章大殿文字消失修复 |
| 🔴 高 | 1.2 | 经验曲线优化（控制最多20次点击升级） |
| 🔴 高 | 1.3 | 多人战斗 NPC 队友 AI 卡住修复 |
| 🟢 新 | 2.1 | NPC 动态系统（数值卡 + 行为逻辑） |
| 🟡 中 | 3.1 | 第三章 CG 图片需求 |
| 🟡 中 | 3.2 | 第三章 NPC 专属立绘需求 |

---

*此文档由 AI 根据用户需求整理生成，供 0429 开发参考。*